export const styles = {
    dividerOutter: (padding) => ({
        width: "100%",
        padding: `${padding} 0`
    }),
    dividerInner: (width, borderWidth) => ({
         width: width,
         borderTop: `${borderWidth} solid #ffffff41`
    })
}
